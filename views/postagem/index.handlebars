<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
 
        <small>Data da Publicação: {{#formatDate postagem.data}}{{/formatDate}}</small><br>
        <hr>
        {{postagem.conteudo}}
    </div>

</div>